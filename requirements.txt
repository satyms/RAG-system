# ── Core ────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ── LangChain (Gemini generation only) ──────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0

# ── Embeddings ──────────────────────────────
sentence-transformers>=3.0.0
torch>=2.0.0

# ── Vector Store (Qdrant) ──────────────────
qdrant-client>=1.9.0

# ── Database (PostgreSQL async) ─────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# ── Document Loaders ───────────────────────
pypdf>=4.0.0
python-docx>=1.0.0
unstructured>=0.15.0

# ── Testing ─────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
